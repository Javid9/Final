@model IEnumerable<Category>
@{
    ViewData["Title"] = "Index";
}

    <div class="row">
        <div class="col-lg-10 grid-margin stretch-card" style="position:absolute;right:0;top:120px">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Category Table</h4>
                    <a asp-action="Create" class="btn btn-success mt-3">Create</a>
                    @if (Model.Count() != 0)
                    {
                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                    <tr>
                                        <th>
                                            Name
                                        </th>
                                        <th>
                                            Icon Class Name
                                        </th>
                                        <th>
                                            Setting
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (Category ctg in Model)
                                    {
                                        <tr>
                                            <td>
                                                @ctg.CategoryName
                                            </td>
                                            <td>
                                                @ctg.IconClassName
                                            </td>
                                            <td>
                                                <a asp-action="Update" asp-route-id="@ctg.Id" class="btn btn-warning">
                                                    <i class="far fa-edit"></i>
                                                </a>
                                                <br /> <br />

                                                <a asp-action="Delete" asp-route-id="@ctg.Id" class="btn btn-danger">
                                                    <i class="fas fa-trash-alt"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        //<h4 class="card-title">Empty Table</h4>
                    }
                </div>
            </div>
        </div>
    </div>

